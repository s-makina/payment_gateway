spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/payment_gateway
    username: postgres
    password: Forcode123!
    driver-class-name: org.postgresql.Driver

  h2:
    console:
      enabled: true
      path: /h2-console

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true

payment:
  gateways:
    onekhusa:
      enabled: true
      base-url: ${ONEKHUSA_BASE_URL:https://api.onekhusa.com/sandbox/v1}
      api-key: ${ONEKHUSA_API_KEY:sandbox_1y9pto5RSBykdybCJnanXgvm1Rg4SiXGmg}
      api-secret: ${ONEKHUSA_API_SECRET:xTEPvKNebsyjS7DlZa0_7qatm54ptmHEEOwravK3igYYyl6criC5jP5sUoUV}
      organisation-id: ${ONEKHUSA_ORGANISATION_ID:TUWT67VBHONB}
      merchant-account-number: ${ONEKHUSA_MERCHANT_ACCOUNT_NUMBER:76684641}
      webhook-secret: ${ONEKHUSA_WEBHOOK_SECRET:}
      captured-by: ${ONEKHUSA_CAPTURED_BY:salvation.developer@gmail.com}
      # No captured-by override here: inherit the fallback from application.yml
      # (${ONEKHUSA_CAPTURED_BY:salvation.developer@gmail.com}) so an unset env
      # var can no longer silently wipe the value to an empty string.
    paychangu:
      enabled: true
      environment: sandbox
      base-url: ${PAYCHANGU_BASE_URL:https://api.paychangu.com}
      secret-key: ${PAYCHANGU_SECRET_KEY:sec-test-K9lZTFw2rLxTdYA5MqSwkqmf6Upari0M}
      webhook-secret: ${PAYCHANGU_WEBHOOK_SECRET:pub-test-hJ0SLTOfNXFCQai8dEipe6ZMjOoOARMh}
      callback-url: ${PAYCHANGU_CALLBACK_URL:https://example.com/payments/callback}
      return-url: ${PAYCHANGU_RETURN_URL:https://example.com/payments/return}
      customization-title: ${PAYCHANGU_CUSTOMIZATION_TITLE:Make Payment}

logging:
  level:
    com.paymentgateway: DEBUG
    org.springframework.web: DEBUG
    org.flywaydb: DEBUG
